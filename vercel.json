{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "export VITE_API_URL=https://fleethq-platform-production.up.railway.app && npm ci && npm run build && npm --prefix admin ci && npm --prefix admin run build && rm -rf dist/admin && mv admin/dist dist/admin",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/admin/(.*)", "destination": "/admin/index.html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
